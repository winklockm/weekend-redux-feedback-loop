import {useState} from 'react';
import {useSelector} from 'react-redux';

function ReviewPage() {

    // const feeling = useSelector(selector => selector.feelReducer);
    // const understanding = useSelector(selector => selector.understandReducer);
    // const support = useSelector(selector => selector.supportReducer);
    // const comments = useSelector(selector => selector.commentReducer);

    // console.log(`feeling: ${feeling}, understanding: ${understanding}, support: ${support}, comments: ${comments}`);
    // const [responses, setResponses] = useState({
    //         feeling,
    //         understanding,
    //         support,
    //         comments
    // })

    return (
        <>
        {/* <p>Here is the ReviewPage</p>
        <p>How are you feeling today?</p>
        <p>{feeling}/5</p>
        <p>How well are you understanding the content?</p>
        <p>{understanding}/5</p>
        <p>How well are you being supported?</p>
        <p>{support}/5</p>
        <p>Any comments you want to leave?</p>
        <p>{comments}/5</p> */}
        </>
    )
}

export default ReviewPage;